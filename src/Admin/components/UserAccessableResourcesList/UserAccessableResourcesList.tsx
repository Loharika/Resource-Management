import React, { Component } from 'react'
import { observer } from 'mobx-react'

@observer
class UserAccessableResourcesList extends Component {
   render() {
      return <div>UserAccessableResourcesList</div>
   }
}

export default UserAccessableResourcesList
